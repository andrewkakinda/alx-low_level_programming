#include	"main.h"

/**
 * _abs - computes the absolute value of integer
 *
 *@i: integer to be compared
 *
 * Return: always 0
 *
 */
int	_abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}

